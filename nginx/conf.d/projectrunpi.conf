server {
    listen 80;
    server_name localhost;

    # Matches the path you mounted in docker-compose, pointing to Laravel's public folder
    root /var/www/projectrunpi/api/public;

    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        # Points to the 'web' service from your docker-compose.yml
        fastcgi_pass web:9000; 
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}